

# Added by cargo

/target

# .NET build outputs (never commit these)
TruckPilot.NET/**/bin/
TruckPilot.NET/**/obj/
TruckPilot.TelemetryDLL/build/

# IDE
.vs/
.vscode/
*.suo
*.user

# Stack traces / temporary logs
*.log
!crates/telemetry/tests/fixtures/*.log
live_test_*.log

# OS
Thumbs.db
.DS_Store

# Misc temporary
truckpilot_bundle.txt
*.tmp
~syncthing~*

# Secrets — NEVER commit
.env
.env.*
!.env.example

# Map-parser test fixtures — extracted from a local ETS2 install,
# regenerated via `cargo test --test extract_fixtures -- --ignored`.
# Not committed because the bytes are derived from copyrighted ETS2 data.
crates/map-parser/tests/fixtures/*.bin

# AI assistant history (per-user, not for the repo)
.aider*
.claude/settings.local.json
.opencode/

# Operator-local one-off scripts (kept on disk, not in VCS)
featherless-models.json
fallback_proxy.py
test-fallbacks.sh
test-models.sh
test-results.md

# Frontend build outputs / deps
crates/ui/node_modules/
crates/ui/dist/
crates/ui/.svelte-kit/
crates/ui/build/
node_modules/

# Compiled plugin DLLs / SOs at top-level (built artifacts)
plugins/*.so
plugins/*.dll
plugins/*.dylib

# Old-Rust-1.x experimentation
hashfs_reader/target/

# Android Gradle wrapper artifacts handled per-project

graph.json
hashes.txt
parse_full.txt

# Phase 5.x diagnostic dumps written by `truckpilot-road-dump` etc.
outputs/

# Diagnostic/tool-generated files at repo root (legacy locations)
parse_output.txt
road_dump_*.txt
skip_audit*.txt
sector_audit*.txt
uid_resolution*.txt
aux_inspection*.txt
*.bak

# Obsidian Vault — komplett ignoriert, Notizen sind privat und lokal
docs/vault/

# Android — Build-Outputs
android-server-monitor/app/build/
android-server-monitor/.gradle/
android-server-monitor/local.properties
android-server-monitor/app/release/

# Pre-built Release-Binaries
release/

# Rustdoc-Output
docs/api/

# Diagnose-Dumps am Repo-Root (zusätzlich zu graph.json/hashes.txt/parse_full.txt)
ets2_nodes.json
ets2_map_data.json
audit_report.txt
*_report.txt
*_results.txt
probe.log
last_route_uids*.txt
mod_order.json
perf.data*

# Python-Build-Output
__pycache__/
*.egg-info/
.venv/

# Release-Archive
*-v*.zip

# .NET / generic build dirs
**/obj/


# Phase 6.5c demo tooling (throwaway)
tools/vision-live-demo/
models/

# Local stats database written by stats-logger plugin during runs
stats.db

# Claude Code worktrees scratch dir
.claude/worktrees/

# Test artifacts
capture_frames/
tools/cvat-setup/vendor/
tools/model-tools/sign_crops/
tools/model-tools/sign_crops_labeled/

# Auto Claude data directory
.auto-claude/

# Auto Claude generated files
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/

# Graphify cache and generated HTML (regenerated on demand)
graphify-out/cache/
graphify-out/*.html

# TypeScript incremental build info
*.tsbuildinfo

# Standalone sub-projects (local only, not part of this repo)
flask/
tools/discord/

# Compiled plugin DLLs / deployment directory (build artifacts, root-level only)
/plugins/

# Named graph snapshots / baselines kept locally
baselines/
