Füge die shadcn-Komponente $ARGUMENTS via `npx shadcn@latest add $ARGUMENTS` hinzu.
Prüfe danach, ob die Komponente in components/ui/ erschienen ist und ob sie in
mindestens einer Route verwendet wird oder werden soll. Falls nicht, frage nach.
