//! `truckpilot-map-probe` — placeholder stub.
//!
//! Reserved entry point for a future interactive map-archive probe
//! (intended to mirror `truckpilot-sector-audit` at archive scope).
//! Currently prints a tag and exits with status 0 so the `[[bin]]` slot
//! in `Cargo.toml` keeps compiling.

fn main() {
    println!("map-probe stub");
}
